.env
.env.*
# 只忽略运行期产物,不要用 *.json —— 那会把 package.json 这类源码配置一起忽略掉
state.json
*.local.json
*.jsonl
*.sqlite*
*.log
__pycache__/
*.py[cod]
.venv/
build/
dist/
*.egg-info/
.DS_Store
# Local development harness runtime (scripts/dev.sh): isolated home, fixture
# source tree, logs, dev app bundle and the seeded .env.
.dev/
