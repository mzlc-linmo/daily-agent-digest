{
  "name": "daily-agent-digest-submit",
  "private": true,
  "type": "module",
  "description": "日报提交服务(Cloudflare Worker):接收 App 提交并写入飞书多维表格",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
