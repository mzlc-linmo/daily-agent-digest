[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "daily-agent-digest"
version = "0.6.0"
description = "Local daily summaries across Codex, Pi agent, and DeepSeek Harness sessions"
requires-python = ">=3.10"

[project.scripts]
daily-agent-digest = "daily_agent_digest:main"
